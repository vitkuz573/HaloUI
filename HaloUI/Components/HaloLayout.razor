<div class="@RootClass" style="@RootStyle" @attributes="AdditionalAttributes">
    @if (BackgroundOverlay is not null)
    {
        <div class="ui-layout__background">
            @BackgroundOverlay
        </div>
    }

    <div class="ui-layout__shell" style="@ShellStyle">
        @if (ShouldRenderOverlay)
        {
            <button type="button"
                    class="ui-layout__backdrop"
                    style="@BackdropStyle"
                    aria-label="@OverlayCloseLabel"
                    @onclick="HandleOverlayCloseAsync"></button>
        }

        @if (Navigation is not null)
        {
            <aside class="@NavigationContainerClass" style="@NavigationStyle" @attributes="NavigationAttributes">
                @Navigation
            </aside>
        }

        <div class="@MainContainerClass" style="@MainStyle" @attributes="MainAttributes">
            @if (Header is not null)
            {
                <header class="@HeaderContainerClass">
                    @Header
                </header>
            }

            @if (Toolbar is not null)
            {
                <div class="@ToolbarContainerClass">
                    @Toolbar
                </div>
            }

            <main class="@ContentContainerClass">
                @ChildContent
            </main>

            @if (Footer is not null)
            {
                <footer class="@FooterContainerClass">
                    @Footer
                </footer>
            }
        </div>

        @if (Notification is not null)
        {
            <aside class="@NotificationContainerClass" style="@NotificationStyle" @attributes="NotificationAttributes">
                @Notification
            </aside>
        }
    </div>
</div>
