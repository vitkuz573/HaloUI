@using HaloUI.Enums

<button type="button"
        class="@GetClasses()"
        disabled="@Disabled"
        @onclick="HandleToggle"
        @attributes="BuildAttributes()">
    <HaloIcon Name="@GetIcon()" Decorative="true" />
</button>
